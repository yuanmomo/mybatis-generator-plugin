<!-- https://logback.qos.ch/manual/appenders.html -->
<configuration scan="true" scanPeriod="30 seconds">

    <contextName>mybatis-generator-plugin</contextName>

    <property name="LOG_HOME" value="./log/"/>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender"> <!-- encoders are assigned the type ch.qos.logback.classic.encoder.PatternLayoutEncoder
			by default -->
        <encoder>
            <!-- <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern> -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %contextName [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>


    <appender name="system" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>system.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <FileNamePattern>${LOG_HOME}/system.log.%d{yyyy-MM-dd}</FileNamePattern>
            <!-- keep 30 days worth of history -->
            <MaxHistory>30</MaxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="system" level="debug">
        <appender-ref ref="system"/>
    </logger>

    <root level="ALL">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>